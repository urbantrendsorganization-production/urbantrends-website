import connectDb from "../../config/database.js";

// this file will handle main company products tiers.

export const addproductTiers = async (req, res) => {
    try {
        const database = await connectDb();
    } catch (error) {
        
    }
}